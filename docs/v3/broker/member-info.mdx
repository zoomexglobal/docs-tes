---
title: Get direct customers information
sidebar_label: Get direct customers information
sidebar_position: 4
---

> API rate limit: 300 req / min

### HTTP Request
GET `/private/v1/broker/member-info`

### Request Parameters
| Parameter | Required | Type | Comments|
|:----- |:-------|:-----|------ |
|uid |**true** |string |Direct customer UID |

### Response Parameters
| Parameter | Type | Comments|
|:----- |:-----|----- |
|is_valid |integer| Whether it is a direct customer, 0: No; 1: Yes|
|signup_time |string| Time to become a direct customer|

### Request Example

```python
       def your_method(apiKey, secretKey):
           timestamp = int(time.time() * 10 ** 3)
           url = 'https://openapi-testnet.zoomex.com/private/v1/broker/member-info'
           method = "GET"
           params = {
               "api_key": apiKey,
               "timestamp": str(timestamp),
               "recv_window": "5000",
               "uid": 101479471,
           }
           param_str = ''
           for key in sorted(params.keys()):
               v = params[key]
               if isinstance(params[key], bool):
                   if params[key]:
                       v = 'true'
                   else:
                       v = 'false'
               param_str += key + '=' + v + '&'
           param_str = param_str[:-1]
           hash = hmac.new(secretKey, param_str.encode("utf-8"), hashlib.sha256)
           signature = hash.hexdigest()
           sign_real = {
               "sign": signature
           }
           param_str = quote_plus(param_str, safe="=&")
           full_param_str = f"{param_str}&sign={sign_real['sign']}"
           urllib3.disable_warnings()
           s = requests.session()
           s.keep_alive = False
           response = requests.request(method, f"{url}?{full_param_str}", verify=False)

       def main():
           apiKey = "Your key"
           secret = "Your secret"
           your_method(apiKey, secret)

```

### Response Example

```json
{
    "ret_code": 0,
    "ret_msg": "OK",
    "ext_code": "",
    "result": {
        "is_valid": 1,
        "signup_time": "2024-06-11 04:16:58 +0000 UTC"
    },
    "ext_info": null,
    "time_now": 1719238958533
}
```
